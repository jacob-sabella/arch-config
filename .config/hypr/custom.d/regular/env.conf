# Custom environment variables

# Use custom hypridle configuration
env = HLDE_HYPRIDLE_CUSTOM,1

# Disable built-in hyprpaper (using awww + hypr-workspace-wallpaper instead)
env = HLDE_NO_HYPRPAPER,1
